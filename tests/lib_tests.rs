mod tests {
    use keyphrases;

    #[test]
    fn test_keywords() {
        let test_string: String = String::from("Feature extraction is not that complex. There are many algorithms available that can help you with feature extraction. Rapid Automatic Keyword Extraction is one of those.");

        assert!(false);
    }
}
